# 案例5 - 时钟管理自动调频测试

测试 `clock_manager` 模块的自动调频功能，通过CPU负载模拟验证自动调频策略�?
---

## 📋 功能说明

### 核心功能

- **自动调频策略**�?  - CPU使用�?< 30%：降�?个系数，最�?MHz，降频间�?�?  - CPU使用�?> 50%：升�?个系数，升频间隔1秒，最�?2MHz
- **CPU负载模拟**�?  - 10秒周期内随机时间创造CPU使用�?0-70%
  - 每次运行1-5秒不�?  - 随机间隔启动
- **OLED显示**�?  - �?行：`CPU: XXX%`（CPU使用率）
  - �?行：`Coeff: X Freq: XXMHz`（当前系数和频率�?  - �?行：`Sim: Running` �?`Sim: Idle`（模拟程序状态）
  - �?行：`Remain: Xs` �?`Next: Xs`（剩余时间或下次启动倒计时）
- **LED1**：每500ms闪烁一次（慢速，用于观察延时�?- **LED2**：闪烁速度与频率成正比（系�?�?�?秒，系数9�?�?秒）

### 自动调频策略详解

| 条件 | 动作 | 间隔 | 限制 |
|------|------|------|------|
| CPU使用�?> 50% | 升频3�?| 1�?| 最�?2MHz（系�?�?|
| CPU使用�?< 30% | 降频1�?| 5�?| 最�?MHz（系�?�?|

### CPU负载模拟

- **周期**�?0秒一个周�?- **启动时间**：随机（0-10秒内�?- **运行时长**：随机（1-5秒）
- **负载强度**�?0-70% CPU使用�?- **实现方式**�?0-70%时间执行空循环，30-40%时间调用空闲钩子

---

## 🔧 硬件要求

### 必需硬件

- STM32F103C8T6开发板
- OLED显示屏（SSD1306，I2C接口�?  - SCL连接到PB8
  - SDA连接到PB9
- LED1连接到PA1
- LED2连接到PA2

### 外部晶振要求

- **有外部晶�?*：可以切换到72MHz，自动调频功能完�?- **无外部晶�?*：自动保�?MHz，调频功能受限（只能使用HSI频率�?
### 硬件配置

**案例5提供了专用的硬件配置文件：`board.h`**

**快速使�?*�?```powershell
# 备份当前配置（可选）
Copy-Item 案例目录下的 board.h 案例目录下的 board.h.backup

# 使用案例配置
Copy-Item Examples\example5_clock_manager\board.h 案例目录下的 board.h -Force

# 复制示例代码
Copy-Item Examples\example5_clock_manager\main_example.c Core\main.c -Force

# 编译运行后，恢复原配置（如果备份了）
Copy-Item 案例目录下的 board.h.backup 案例目录下的 board.h -Force
```

**配置内容**�?- LED配置（PA1和PA2�?- OLED I2C配置（PB8和PB9�?- 时钟管理配置（自动调频策略参数）

**注意**�?- `board.h` 只包含本案例需要的配置，复制后即可编译
- 如果硬件引脚不同，可以手动修�?`案例目录下的 board.h` 中的引脚配置

---

## 🚀 使用方法

### 快速开�?
1. **复制案例配置和代�?*�?   ```powershell
   Copy-Item Examples\example5_clock_manager\board.h 案例目录下的 board.h -Force
   Copy-Item Examples\example5_clock_manager\main_example.c Core\main.c -Force
   ```
2. **编译下载**：在Keil中编译（F7）并下载（F8）到开发板
3. **观察效果**�?   - OLED显示CPU使用率、系数、频率、任务状态、跑秒器、平均MHz
   - LED1固定500ms闪烁
   - LED2闪烁速度随频率变化（频率高闪得快，频率低闪得慢）

### 详细操作流程

**通用操作步骤请参�?*：[Examples/README.md](../README.md#-通用操作流程)

---

## 🎯 预期效果

### 正常现象

1. **初始化阶�?*�?   - OLED显示初始化信�?   - 检测HSE并尝试切换到72MHz
   - 设置为自动模�?
2. **运行阶段**�?   - **OLED显示**�?     - CPU使用率在0%-100%之间变化
     - 系数�?-9之间变化（根据负载自动调整）
     - 频率�?MHz-72MHz之间变化
     - 模拟程序状态在"Running"�?Idle"之间切换
   - **LED1**：固�?00ms闪烁（慢速）
   - **LED2**�?     - 高负载时：闪烁变快（频率升高，系数减小）
     - 低负载时：闪烁变慢（频率降低，系数增大）

3. **自动调频行为**�?   - 模拟程序运行时（60-70%负载）：CPU使用�?50%�?秒后升频3�?   - 模拟程序停止后（低负载）：CPU使用�?30%�?秒后降频1�?   - 频率�?MHz-72MHz之间动态调�?
### 测试现象判断�?
| 测试现象 | 说明 |
|---------|------|
| **OLED显示频率变化，LED2闪烁速度变化** | �?**成功�?* 自动调频功能正常工作 |
| **模拟程序运行时频率上升，停止后频率下�?* | �?**成功�?* 调频策略正常工作 |
| **OLED显示固定频率，LED2闪烁速度不变** | ⚠️ **检�?*：可能是负载模拟未生效，或时钟切换失�?|
| **OLED不显示或显示异常** | �?**失败�?* 检查OLED连接和配�?|
| **LED不亮或常�?* | �?**失败�?* 检查LED引脚配置和硬�?|

---

## 📝 代码说明

### 关键函数

#### `CLKM_Init()`
- 初始化时钟管理模�?- 设置默认频率�?2MHz（系�?�?
#### `CLKM_SetMode(CLKM_MODE_AUTO, CLKM_LVL_8MHZ)`
- 设置为自动模�?- 最低频率为8MHz（系�?�?
#### `CLKM_AdaptiveTask()`
- 自适应任务，每50ms检测一次CPU负载
- 根据负载自动调整频率
- 新策略：<30%降频1�?5秒，>50%升频3�?1�?
#### `CLKM_IdleHook()`
- 空闲钩子函数
- 在低负载时调用，统计空闲时间

#### `SimulateHighLoad(duration_ms)`
- 模拟CPU高负载（60-70%使用率）
- 在指定时间内�?0-70%时间执行空循环，30-40%时间调用空闲钩子

### 代码流程

```c
// 1. 系统初始�?System_Init();              // 初始化延时、LED�?OLED_Init();                // 初始化OLED

// 2. 时钟管理初始�?CLKM_Init();                // 初始化时钟管理模�?CLKM_SetMode(CLKM_MODE_MANUAL, CLKM_LVL_72MHZ);  // 先切换到72MHz
CLKM_SetMode(CLKM_MODE_AUTO, CLKM_LVL_8MHZ);     // 设置为自动模�?
// 3. 主循�?while(1) {
    // CPU负载模拟�?0秒周期内随机启动，运�?-5秒）
    if (sim_running) {
        SimulateHighLoad(50);  // 模拟高负�?    } else {
        CLKM_IdleHook();       // 低负�?    }
    
    // 自动调频
    CLKM_AdaptiveTask();
    
    // 更新OLED显示
    // 控制LED闪烁
}
```

---

## 🔍 技术细�?
### 频率档位�?
| 系数 | 频率 | 时钟�?| PLL倍频 | Flash等待周期 |
|------|------|--------|---------|---------------|
| 1 | 72MHz | PLL | x9 | 2 |
| 2 | 64MHz | PLL | x8 | 2 |
| 3 | 56MHz | PLL | x7 | 2 |
| 4 | 48MHz | PLL | x6 | 1 |
| 5 | 40MHz | PLL | x5 | 1 |
| 6 | 32MHz | PLL | x4 | 1 |
| 7 | 24MHz | PLL | x3 | 0 |
| 8 | 16MHz | PLL | x2 | 0 |
| 9 | 8MHz | HSI | - | 0 |

### 自动调频策略

- **高负载阈�?*�?50%
- **低负载阈�?*�?30%
- **升频策略**：跳3档（快速响应）
- **降频策略**：降1档（渐进式）
- **升频间隔**�?�?- **降频间隔**�?�?- **检测周�?*�?0ms

### CPU负载模拟算法

1. **10秒周�?*：每10秒重新开�?2. **随机启动时间**�?-10秒内随机
3. **随机运行时长**�?-5秒随�?4. **负载强度**�?0-70% CPU使用�?   - 60-70%时间：执行空循环（`for(i=0; i<50000; i++);`�?   - 30-40%时间：调用`CLKM_IdleHook()`和短暂延�?
### 为什么LED2闪烁速度会变化？

1. **频率变化**：从72MHz降到8MHz�?倍差异）
2. **系数变化**：从系数1到系�?
3. **LED2闪烁速度**：`led2_blinks_per_sec = 10 - coefficient`
   - 系数1：每秒闪9次（111ms间隔�?   - 系数9：每秒闪1次（1000ms间隔�?4. **实际效果**：频率越高，系数越小，LED2闪烁越快

---

## ⚠️ 常见问题

### OLED不显�?
- 检查OLED连接（SCL=PB8, SDA=PB9�?- 检�?`案例目录下的 board.h` 中OLED配置
- 检查OLED初始化是否成�?
### LED不闪�?
- 检�?`案例目录下的 board.h` 中LED配置
- 检查LED引脚是否与硬件连接一�?- 检查LED有效电平配置

### 频率不变�?
- 检查负载模拟是否生效（OLED应显�?Sim: Running"�?- 检�?`CLKM_AdaptiveTask()` 是否被调�?- 检查CPU负载统计是否正常（OLED应显示CPU使用率）
- 检查是否有外部晶振（无外部晶振时只能使用HSI频率�?- 检查调频间隔是否满足（升频1秒，降频5秒）

### CPU使用率始终为0�?00%

- 检�?`CLKM_IdleHook()` 是否在低负载时被调用
- 检查负载模拟函数是否正常工�?- 检�?`g_task_tick` 是否正常递增

### 编译错误

- 确保已包含必要的头文�?- 确保 `clock_manager.c/h` 已添加到项目
- 确保 `TIM2_TimeBase.c/h` 已添加到项目

---

## 🔬 扩展练习

1. **修改负载阈�?*：调�?`CLKM_LOAD_THRESHOLD_HIGH` �?`CLKM_LOAD_THRESHOLD_LOW`
2. **修改切换间隔**：调�?`CLKM_SWITCH_INTERVAL_UP` �?`CLKM_SWITCH_INTERVAL_DOWN`
3. **修改切换策略**：调整升�?降频的档位数
4. **使用实际负载**：替换模拟负载为实际任务负载
5. **添加手动模式**：允许手动切换频率档�?
---

## 📚 相关模块

- **时钟管理**：`system/clock_manager.c/h`
- **延时功能**：`system/delay.c/h`
- **基时定时�?*：`Drivers/timer/TIM2_TimeBase.c/h`
- **OLED驱动**：`Drivers/display/oled_ssd1306.c/h`
- **LED驱动**：`Drivers/basic/led.c/h`
- **系统初始�?*：`system/system_init.c/h`
- **硬件配置**：`案例目录下的 board.h`

---

## 💡 设计亮点

1. **智能调频**：根据CPU负载自动调整频率，节省功�?2. **渐进式降�?*：降频时逐步降低，避免突然变�?3. **快速升�?*：高负载时快速升频，保证性能
4. **可视化反�?*：OLED显示实时状态，LED闪烁速度反映频率变化
5. **随机负载模拟**�?0秒周期内随机启动，更真实地模拟实际应用场�?6. **超时保护**：时钟切换时使用超时保护，不会卡�?
---

**最后更�?*�?024-01-01（重构完成：新自动调频策略，CPU负载模拟�?